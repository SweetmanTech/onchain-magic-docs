import { Callout } from "nextra-theme-docs";

# use1155Drop

The useFeed hook provides functions for using a zora feed.

once you've installed `onchain-magic` you can use the `use1155Drop` hook in your app

### Usage

```
import { use1155Drop } from 'onchain-magic';
const ZoraDrop = () => {
    const { tokens, mintAll, mintWithRewards } = use1155Drop()

    return(
        <div>
            {tokens.map((token) => (
                <div>
                    <p>Metadata: {token.uri}</p>
                    <button type="button" onClick={mintWithRewards(token.tokenId)}>Collect</button>
                </div>
            ))}
            <button type="button" onClick={mintAll}>Collect All</button>
        </div>
    )
}

export default ZoraDrop
```

### Drop

the tokens are an array of `drops`.

Each Drop contains the following attributes:

| Parameter           | Description                                          | Type        |
| ------------------- | ---------------------------------------------------- | ----------- |
| **contractAddress** | The address of the token contract.                   | string      |
| **chainId**         | The id of the chain on which the media exists        | string      |
| **uri**             | The content uri of the media                         | string      |
| **blockNumber**     | The blocknumber when the media was published onchain | string      |
| **type**            | Contract Type                                        | 1155 or 721 |
| **tokenId**         | The token ID.                                        | string      |
